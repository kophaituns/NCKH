import React from 'react';
import WorkspaceList from './WorkspaceList';

/**
 * Main Workspaces page component
 * Renders the workspace list with create functionality
 */
const Workspaces = () => {
  return <WorkspaceList />;
};

export default Workspaces;